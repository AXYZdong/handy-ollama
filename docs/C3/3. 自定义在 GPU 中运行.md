# Ollama 自定义在 GPU 中运行

## Windows

Ollama 会自动检测并使用受支持的 GPU，不需要额外设置用于“启用 CUDA”或“指定 GPU 层数”的 Ollama 环境变量。如果电脑有多张 NVIDIA GPU，可以通过 `CUDA_VISIBLE_DEVICES` 限制 Ollama 可见的设备。

### 前提条件

- 电脑配有 [Ollama 支持的 NVIDIA GPU](https://docs.ollama.com/gpu#nvidia)。
- 已安装符合当前 Ollama 要求的 NVIDIA 显卡驱动；可以运行 `nvidia-smi` 检查驱动和显卡信息。具体支持范围和最低驱动版本请以官方 GPU 文档为准。
- 已按照 [Ollama Windows 官方文档](https://docs.ollama.com/windows) 安装并启动 Ollama。

> [!TIP]
> 标准的 Ollama Windows 安装会自行选择可用的计算后端。`OLLAMA_GPU_LAYER` 和 `OLLAMA_GPU_LAYERS` 都不是 Ollama 支持的服务器环境变量。

### （可选）指定 NVIDIA GPU

1. **查找 GPU UUID**

   打开命令提示符或 PowerShell，运行：

   ```powershell
   nvidia-smi -L
   ```

   从输出中找到目标 GPU 的 UUID，例如 `GPU-xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx`。与数字编号相比，UUID 不容易因驱动更新或系统重启而改变。

   ![](../images/C3-3-2.png)

2. **创建 `CUDA_VISIBLE_DEVICES` 用户环境变量**

   - 先从 Windows 任务栏退出正在运行的 Ollama。
   - 在 Windows 搜索栏中搜索“环境变量”，打开“编辑帐户的环境变量”。
   - 在“用户变量”区域新建 `CUDA_VISIBLE_DEVICES`，值填写目标 GPU 的 UUID；需要选择多张 GPU 时使用英文逗号分隔。
   - 保存后，从 Windows 开始菜单重新启动 Ollama。

> [!IMPORTANT]
> Ollama 只会在进程启动时读取环境变量。修改变量后，仅重新打开命令提示符并不足以更新已经运行的 Ollama；需要先退出 Ollama，再重新启动 Ollama。

**验证 GPU 加速是否生效：**

1. 打开命令提示符或 PowerShell。
2. 运行模型，例如：`ollama run deepseek-r1:1.5b`。
3. 保持模型加载，在另一个终端运行 `ollama ps`。
4. 查看 `PROCESSOR` 列：`100% GPU` 表示模型完全加载到 GPU；`100% CPU` 表示完全加载到系统内存；混合百分比表示部分加载到 GPU。

![](../images/C3-3-3.png)

## Linux

以下以 Linux 系统为例，介绍如何自定义在 GPU 中运行 Ollama。

1. 创建 `ollama_gpu_selector.sh` 脚本文件，内容如下：

```bash
#!/bin/bash

# Validate input
validate_input(){
if[[! $1 =~^[0-4](,[0-4])*$ ]];then
        echo "Error: Invalid input. Please enter numbers between 0 and 4, separated by commas."
exit1
fi
}

# Update the service file with CUDA_VISIBLE_DEVICES values
update_service(){
# Check if CUDA_VISIBLE_DEVICES environment variable exists in the service file
if grep -q '^Environment="CUDA_VISIBLE_DEVICES='/etc/systemd/system/ollama.service;then
# Update the existing CUDA_VISIBLE_DEVICES values
        sudo sed -i 's/^Environment="CUDA_VISIBLE_DEVICES=.*/Environment="CUDA_VISIBLE_DEVICES='"$1"'"/'/etc/systemd/system/ollama.service
else
# Add a new CUDA_VISIBLE_DEVICES environment variable
        sudo sed -i '/\[Service\]/a Environment="CUDA_VISIBLE_DEVICES='"$1"'"'/etc/systemd/system/ollama.service
fi

# Reload and restart the systemd service
    sudo systemctl daemon-reload
    sudo systemctl restart ollama.service

    echo "Service updated and restarted with CUDA_VISIBLE_DEVICES=$1"
}

# Check if arguments are passed
if["$#"-eq 0];then
# Prompt user for CUDA_VISIBLE_DEVICES values if no arguments are passed
    read -p "Enter CUDA_VISIBLE_DEVICES values (0-4, comma-separated): " cuda_values
    validate_input "$cuda_values"
    update_service "$cuda_values"
else
# Use arguments as CUDA_VISIBLE_DEVICES values
    cuda_values="$1"
    validate_input "$cuda_values"
    update_service "$cuda_values"
fi
```

2. 为脚本文件添加执行权限

```bash
chmod +x ollama_gpu_selector.sh
sudo ./ollama_gpu_selector.sh
```

运行脚本后，按照提示输入 GPU 编号，即可指定 Ollama 使用的 GPU。可以使用逗号分隔多个 GPU 编号，例如：`0,1,2`。

3. 重启 Ollama 服务

```bash
cat /etc/systemd/system/ollama.service
```

运行指令后，查看 Ollama 服务文件，确认 `CUDA_VISIBLE_DEVICES` 环境变量已经更新。

如果已经更新，则会新增例如 `Environment="CUDA_VISIBLE_DEVICES=0,1,2"` 的环境变量。



参考资料

- https://docs.ollama.com/windows
- https://docs.ollama.com/gpu
- https://docs.ollama.com/faq#how-do-i-configure-ollama-server
- https://docs.ollama.com/faq#how-can-i-tell-if-my-model-was-loaded-onto-the-gpu
- https://www.cnblogs.com/hujunwei/p/18706538
- https://mp.weixin.qq.com/s/BQhsiy5aZO1CyIBPPSeG4A
